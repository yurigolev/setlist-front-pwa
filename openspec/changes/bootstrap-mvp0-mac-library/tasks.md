# Tasks

## 1. Устанавливаемая PWA и надёжное хранение — короткий заход 1

- [x] 1.1 Создать Vue 3 + TypeScript + Vite PWA, подключить Router, Pinia, manifest и service worker; проверить, что `npm run build` успешен, а приложение устанавливается из Safari на iPhone
- [x] 1.2 Описать доменные типы `Song` и `Attachment`, интерфейс локального репозитория и IndexedDB-реализацию метаданных; проверить unit-тестами создание и чтение минимальной песни
- [x] 1.3 Добавить хранение Blob-вложений отдельным адаптером IndexedDB, запрос persistent storage и статус `StorageManager.estimate()`; проверить unit-тестом обработку отказа/ошибки записи без ложного сохранения файла
- [x] 1.4 Проверить app shell на целевом iPhone: выполнить первый запуск с сетью, закрыть PWA, включить авиарежим и убедиться, что интерфейс библиотеки открывается

## 2. Библиотека и карточка песни на iPhone — короткий заход 2

- [x] 2.1 Реализовать маршрут библиотеки с пустым состоянием и переходом к форме новой песни; проверить в установленной PWA на iPhone переход из пустой библиотеки к созданию
- [x] 2.2 Реализовать форму названия, исполнителя и свободных заметок с валидацией названия; проверить, что пустое название показывает ошибку, а минимальная песня сохраняется
- [x] 2.3 Реализовать экран песни, редактирование текстовых полей и статус надёжности хранилища; проверить на iPhone, что после закрытия и повторного запуска PWA отображаются изменённые данные и актуальный статус хранения

## 3. Скриншоты и плейбэки на устройстве — короткий заход 3

- [ ] 3.1 Добавить системный выбор файлов на iPhone и drag-and-drop для Mac для PNG/JPEG/WebP/MP3 с сообщением о неподдерживаемых форматах; проверить на iPhone добавление PNG и MP3 и отклонение TXT
- [x] 3.2 Связать принятые файлы с черновиком и сохранённой песней через локальный адаптер; проверить после закрытия и повторного запуска PWA на iPhone, что изображение и MP3 остаются доступными в авиарежиме
- [x] 3.3 Показать изображения на карточке песни и добавить HTML-аудиоплеер для MP3; проверить на iPhone в авиарежиме, что изображение открывается и MP3 запускается без запросов к сети

## 4. Поиск и офлайн-путь на iPhone — короткий заход 4

- [x] 4.1 Реализовать поиск без сетевых вызовов по нормализованным названию, исполнителю и заметкам; проверить автотестами поиск в каждом поле, нечувствительность к регистру и пустой результат
- [ ] 4.2 Добавить мгновенное обновление списка при вводе и читаемое пустое состояние; проверить вручную в установленной PWA на библиотеке не менее 20 песен
- [ ] 4.3 Провести контрольный сценарий на iPhone с 20–30 реальными песнями и несколькими MP3: сохранение → закрытие PWA → авиарежим → поиск → просмотр скрина → воспроизведение; зафиксировать длительность добавления, использованный объём и доступную квоту

## 5. Приёмка PWA-first MVP 0 — короткий заход 5

- [ ] 5.1 Добавить smoke-тесты ключевых пользовательских путей: создание, повторное открытие, поиск, предпросмотр и воспроизведение; проверить успешный запуск набора тестов
- [ ] 5.2 Проверить production-сборку и офлайн-сценарий в установленной PWA на целевом iPhone; подтвердить, что тестовая библиотека переживает перезапуск и доступна без сети
- [ ] 5.3 Зафиксировать PWA go/no-go: подтвердить PWA, если офлайн-приёмка успешна и хранилище укладывается в доступную квоту; иначе подготовить отдельный change для экспорта, синхронизации или нативного контейнера
